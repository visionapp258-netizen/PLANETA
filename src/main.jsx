import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './Planeta.jsx' // Certifica-te que o nome do ficheiro é exatamente este

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)

